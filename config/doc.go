// Package config provides configuration management powered by Viper.
//
// It loads YAML configuration files and exposes type-safe accessors for
// server, log, database, cache, JWT, CORS, captcha, and file upload settings.
// Supports hot-reload and custom configuration sections via Custom[T]().
package config
