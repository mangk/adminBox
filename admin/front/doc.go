// Package front embeds and serves the AdminBox Vue frontend UI.
//
// The frontend is compiled into the binary via Go's embed directive.
// Import this package with a blank identifier to register the UI routes
// on the httpServer automatically.
package front
